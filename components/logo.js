import React from "react";
import Image from "next/image";

const Logo = (props) => {
  //   return <Image alt="Logo" src="/static/logo.svg" {...props} />;
  return <Image alt="Logo" src="/logo.jpeg" {...props} />;
};

export default Logo;
